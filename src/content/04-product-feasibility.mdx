import { VerificationSpectrumArchitecture } from '../components/AiBlockchainFoundations'
import { LessonChapter } from '../components/LessonChapter'
import { PoiSecurityArchitecture } from '../components/ProductMechanismArchitecture'
import {
  ChainArchitectureSimple,
  PrivacyArchitecture,
} from '../components/SimplifiedArchitecture'

<LessonChapter
  id="verification-engine"
  className="verification-spectrum-screen"
  eyebrow="04 · FEASIBILITY / RESULT VERIFICATION"
  title="模型不能全网重跑"
  accent="但结果可以分层验证"
  body="独立复算、挑战、TEE、零知识证明和业务验收覆盖不同命题；合约按任务风险选择组合。"
  figureLabel="FEASIBILITY / COST · PRIVACY · ASSURANCE"
  visual={<VerificationSpectrumArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="private-evidence"
  className="privacy-screen"
  eyebrow="04 · FEASIBILITY / PRIVATE EVIDENCE"
  title="原始数据不公开"
  accent="执行仍可被证明"
  body="敏感输入留在隔离环境，协议只接收输入承诺、模型版本、执行度量、结果根和可验证证明。"
  figureLabel="FEASIBILITY / PRIVATE INPUT · PUBLIC PROOF"
  visual={<PrivacyArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="deployment-modes"
  className="chains-screen"
  eyebrow="04 · FEASIBILITY / DEPLOYMENT"
  title="不必先造一条新链"
  accent="三种模式逐步部署"
  body="ACVM 可以先作为链外协处理器，再成为原生执行器，最后按规模需要演进为 PoI 应用链。"
  figureLabel="FEASIBILITY / COPROCESSOR → NATIVE VM → APPLICATION CHAIN"
  visual={<ChainArchitectureSimple />}
>
</LessonChapter>

<LessonChapter
  id="security-boundaries"
  className="security-boundaries-screen"
  eyebrow="04 · FEASIBILITY / ATTACK SURFACE"
  title="可行性的关键"
  accent="是把作弊路径写进协议"
  body="自造需求、重复计分、伪造结果、验证者串谋和任务难度不可比，都要有独立控制与审计指标。"
  figureLabel="SECURITY / THREAT → CONTROL → RESIDUAL RISK"
  visual={<PoiSecurityArchitecture />}
>
</LessonChapter>

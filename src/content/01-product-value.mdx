import {
  GeoVerificationArchitecture,
  SocialSimulationArchitecture,
} from '../components/CoreUseCaseAnimations'
import { LessonChapter } from '../components/LessonChapter'
import { ProductDefinitionArchitecture } from '../components/ProductNarrativeArchitecture'
import { GeoPoiDecisionArchitecture } from '../components/ProductEvidenceArchitecture'

<LessonChapter
  id="product-snapshot"
  className="product-snapshot-screen"
  eyebrow="01 · PRODUCT / PAYMENT CONDITION"
  title="为什么不按调用量结算？"
  accent="调用计成本，验收结果才付款"
  body="ACVM 记录任务、证据、裁决和付款条件。模型仍在链下运行，结果通过验收并达到终局后，托管资金才释放。"
  figureLabel="PRODUCT / OUTCOME SPEC → EVIDENCE → VERDICT → PAY"
  visual={<ProductDefinitionArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="geo-verification"
  className="core-use-case-screen geo-verification-screen"
  eyebrow="01 · PRODUCT / GEO VERIFICATION"
  title="品牌方怎么确认 GEO 真有效？"
  accent="冻结口径，独立复测，按增量付款"
  body="ACVM 先锁定问题集、基线和观察窗口。优化方不能给自己打分；独立节点复测，达到门槛才付款。PoI 不参与这一步。"
  figureLabel="CORE CASE / DEMAND → INFERENCE → OBSERVATION → SETTLEMENT"
  visual={<GeoVerificationArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="simulation"
  className="core-use-case-screen simulation-screen"
  eyebrow="01 · PRODUCT / SOCIAL SIMULATION"
  title="数据不能公开，结论怎么让人信？"
  accent="个体数据留在本地，只公开统计和证明"
  body="ACVM 固定模型、样本承诺和随机过程。雾节点在隔离域运行模拟，Validator 复核聚合结果，不读取个人轨迹。"
  figureLabel="CORE CASE / SEALED INPUT → VERIFIED SIMULATION → SETTLEMENT"
  visual={<SocialSimulationArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="geo-poi-boundary"
  className="geo-poi-boundary-screen"
  eyebrow="01 · PRODUCT / NECESSITY"
  title="GEO 一定要用 PoI 吗？"
  accent="单笔业务不需要；开放协作网络才需要"
  body="GEO 必需的是冻结口径、独立复测和按结果结算。只有开放 Worker、跨机构 Validator 和网络激励需要统一贡献账时，才启用 PoI。"
  figureLabel="DECISION / VERIFIABLE RESULT FIRST · POI ONLY WHEN NEEDED"
  visual={<GeoPoiDecisionArchitecture />}
>
</LessonChapter>

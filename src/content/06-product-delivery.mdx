import { A3sBoxCapabilitiesArchitecture, A3sPowerCapabilitiesArchitecture } from '../components/A3sCapabilitiesArchitecture'
import { LessonChapter } from '../components/LessonChapter'
import { AcvmIntegrationArchitecture } from '../components/ProductEvidenceArchitecture'

<LessonChapter
  id="a3s-box"
  className="a3s-box-screen"
  eyebrow="06 / A3S 智能体执行栈"
  title="A3S 把加密智能体安全送入 TEE"
  accent="a3s-code 构建，a3s-use 组装，a3s-box 运行"
  body="远程证明通过后，镜像和能力才在 TEE 内加载。"
  detail="a3s-code 把智能体逻辑、配置和能力声明构建为可签名镜像；a3s-box 在选中的雾节点核验镜像与 TEE 证明，证明通过后才在可信内存解密。运行中的 a3s-use 按 capabilityRoot 拉取、验签并挂载本次任务授权的远程模型、工具和数据连接器。"
  visual={<A3sBoxCapabilitiesArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="a3s-power"
  className="a3s-power-screen"
  eyebrow="06 / a3s-power 隐私推理"
  title="a3s-power 按层加载大模型"
  accent="当前层进入内存，用完立即释放"
  body="大模型不必一次装进可信内存。"
  detail="a3s-use 把模型推理能力挂载到智能体后，a3s-power 在 TEE 内完成实际推理。模型参数不一次全部进入可信内存，只加载正在计算的一层，完成后立即清零并加载下一层；数据、模型密钥和执行过程始终位于受保护环境。"
  visual={<A3sPowerCapabilitiesArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="deployment-modes"
  className="chains-screen"
  eyebrow="06 / 对接国内区块链"
  title="ACVM 支持原生链与国内链接入"
  accent="两种部署路径，共用同一套任务与验证语义"
  body="原生链内置 Runtime，现有链通过 Driver 接入。"
  detail="Rust 原生链可直接运行 ACVM Runtime；长安链、FISCO BCOS、星火链网或 BSN 则通过 ChainAdapter 记录任务根、裁决根、PoI 根和结算终局。模型与原始数据始终留在 A3S 执行域。"
  figureLabel="原生 Rust 链内置 Runtime / 现有国内链通过 ChainAdapter 接入"
  visual={<AcvmIntegrationArchitecture />}
>
</LessonChapter>

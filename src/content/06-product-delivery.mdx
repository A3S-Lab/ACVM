import { A3sBoxCapabilitiesArchitecture, A3sPowerCapabilitiesArchitecture } from '../components/A3sCapabilitiesArchitecture'
import { AcvmNativeChainArchitecture } from '../components/AcvmNativeChainArchitecture'
import { AgentResolutionArchitecture } from '../components/AgentResolutionArchitecture'
import { FogInferenceArchitecture } from '../components/FogInferenceArchitecture'
import { LessonChapter } from '../components/LessonChapter'
import { AcvmIntegrationArchitecture } from '../components/ProductEvidenceArchitecture'

<LessonChapter
  id="ans"
  className="ans-screen"
  eyebrow="06 · APPENDIX / SERVICE DISCOVERY"
  title="ANS 提供可验证的服务发现"
  accent="签名服务卡绑定服务身份"
  body="调用方核验签名与撤销状态；ANS 负责发现，ACVM 负责验收结算。"
  figureLabel="APPENDIX / SIGNED SERVICE CARD → VERIFIED A2A ENDPOINT"
  visual={<AgentResolutionArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="fog-inference"
  className="fog-screen"
  eyebrow="06 · APPENDIX / FOG COMPUTING"
  title="雾节点让数据本地执行、结果可验证"
  accent="数据留现场，结果可验证"
  body="任务在隔离雾节点执行，回执绑定模型、环境和 taskId，ACVM 验证结果。"
  figureLabel="APPENDIX / LOCAL DATA → NEARBY EXECUTION → VERIFIABLE RECEIPT"
  visual={<FogInferenceArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="a3s-box"
  className="a3s-box-screen"
  eyebrow="06 · APPENDIX / A3S-BOX"
  title="a3s-box 固定任务的隔离执行边界"
  accent="镜像、资源与生命周期统一管理"
  body="默认使用专用内核 MicroVM，显式选择 Sandbox；运行中不静默切换后端。"
  visual={<A3sBoxCapabilitiesArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="a3s-power"
  className="a3s-power-screen"
  eyebrow="06 · APPENDIX / A3S-POWER"
  title="a3s-power 提供隐私计算与参数流式推理"
  accent="TEE 保密执行，活跃层参数按层加载"
  body="当前层参数进入可信内存，计算后立即释放；峰值内存约为单层规模，并输出可核验回执。"
  visual={<A3sPowerCapabilitiesArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="deployment-modes"
  className="chains-screen"
  eyebrow="06 · IMPLEMENTATION / A3S + CHAIN"
  title="A3S 接入国内区块链实现 ACVM"
  accent="近期复用现有账本、身份与终局"
  body="a3s-box 执行，a3s-power 出具证明；ACVM 验收并计算 PoI，ChainAdapter 提交链上最小状态。"
  figureLabel="IMPLEMENTATION / A3S-BOX → A3S-POWER → ACVM → DOMESTIC CHAIN"
  visual={<AcvmIntegrationArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="native-chain"
  className="native-chain-screen"
  eyebrow="07 · NATIVE CHAIN / RUST"
  title="PoI 推理计算直接服务链上 ACVM"
  accent="真实合约需求产生有效推理工作量"
  body="ACVM 异步发布推理任务；验收结果恢复合约状态，同时完成结算、PoI 计量与有界候选权重更新。"
  figureLabel="ACVM TASK → POI INFERENCE SERVICE → ACCEPTED RESULT → VALID POI"
  visual={<AcvmNativeChainArchitecture />}
>
</LessonChapter>

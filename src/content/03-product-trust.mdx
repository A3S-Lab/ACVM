import { AcvmServiceWorkflowArchitecture } from '../components/AcvmServiceWorkflow'
import { AgentResolutionArchitecture } from '../components/AgentResolutionArchitecture'
import { VerificationSpectrumArchitecture } from '../components/AiBlockchainFoundations'
import { ContractModelArchitecture } from '../components/FormalSpecArchitecture'
import { FogInferenceArchitecture } from '../components/FogInferenceArchitecture'
import { LessonChapter } from '../components/LessonChapter'
import { PoiConsensusArchitecture } from '../components/ProductMechanismArchitecture'
import { IntelligenceProofArchitecture } from '../components/SimplifiedArchitecture'

<LessonChapter
  id="system-architecture"
  className="system-architecture-screen"
  eyebrow="03 · WORKFLOW / ONE ORDER"
  title="AP2、A3S 与 ACVM 怎么协作？"
  accent="授权、执行、裁决各由一层负责"
  body="AP2 提供签名授权，A3S 编排并执行任务，ACVM 固定验收规则并生成 PoI；现有链或 AVS 负责终局与结算。"
  figureLabel="STACK / AUTHORIZATION → A3S EXECUTION → ACVM POI → FINALITY"
  visual={<AcvmServiceWorkflowArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="ans"
  className="ans-screen"
  eyebrow="03 · WORKFLOW / ANS"
  title="调用方怎么找到可信 Agent？"
  accent="ANS 解析身份、能力、价格和信誉"
  body="输入一个智能体名称，ANS 返回签名服务记录和 A2A 端点。调用方核验有效期与历史回执后，再签任务。"
  figureLabel="ANS / NAME → VERIFIED SERVICE RECORD → A2A INTENT"
  visual={<AgentResolutionArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="agentic-contract"
  className="formal-screen contract-spec-screen"
  eyebrow="03 · WORKFLOW / AGENTIC CONTRACT"
  title="谁来管预算、权限和失败？"
  accent="智能体合约把任务写成状态机"
  body="合约固定输入输出、Worker、Validator、挑战期和付款条件。任务靠签名回执推进，执行方不能自行宣布完成。"
  figureLabel="CONTRACT / SCHEMA · WORKER · VALIDATOR · SETTLEMENT"
  visual={<ContractModelArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="fog-inference"
  className="fog-screen"
  eyebrow="03 · WORKFLOW / FOG COMPUTING"
  title="数据不出域，推理怎么交付？"
  accent="雾节点就近执行，链上只收证据"
  body="ACVM 按地域、延迟、硬件证明和价格选择 Worker。原始数据留在业务现场，结果根、运行证明和裁决进入账本。"
  figureLabel="FOG / LOCAL DATA → NEARBY INFERENCE → GLOBAL VERIFICATION"
  visual={<FogInferenceArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="verification-engine"
  className="verification-spectrum-screen"
  eyebrow="03 · WORKFLOW / RESULT VERIFICATION"
  title="开放式 AI 结果怎么验？"
  accent="先写验收条件，再组合合适证据"
  body="ACVM 按任务选择复算、挑战、TEE、零知识证明或业务验收。技术证明运行过程，业务规则判断结果是否达标。"
  figureLabel="WORKFLOW / EXECUTION EVIDENCE → BUSINESS VERDICT"
  visual={<VerificationSpectrumArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="poi-proof"
  className="intelligence-screen"
  eyebrow="03 · WORKFLOW / PROOF OF INTELLIGENCE"
  title="什么样的推理才算贡献？"
  accent="真实订单、通过验收、执行可查、不能重放"
  body="ACVM 同时检查需求签名、结果裁决、执行证明和 taskKey。四项都通过才生成 ValidPoI；没有 PoI，就不能领取结果费。"
  figureLabel="POI / DEMAND · RESULT · EXECUTION · FRESHNESS"
  visual={<IntelligenceProofArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="poi-consensus"
  className="poi-consensus-screen"
  eyebrow="03 · WORKFLOW / BOOKKEEPING RIGHT"
  title="PoI 会直接买下记账权吗？"
  accent="不会：PoI 加权，VRF 抽签，BFT 终局"
  body="有效 PoI 先归一、封顶和衰减，只影响被抽中的概率。提议者仍要接受其他节点验块和多数确认。"
  figureLabel="CONSENSUS / POI WEIGHT → VRF PROPOSER → BFT FINALITY"
  visual={<PoiConsensusArchitecture />}
>
</LessonChapter>

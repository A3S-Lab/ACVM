import { GeoVerificationArchitecture, SocialSimulationSimpleArchitecture } from '../components/CoreUseCaseAnimations'
import { AgentResolutionArchitecture } from '../components/AgentResolutionArchitecture'
import { FogInferenceArchitecture } from '../components/FogInferenceArchitecture'
import { LessonChapter } from '../components/LessonChapter'
import { PoiConsensusArchitecture } from '../components/ProductMechanismArchitecture'
import { AgentRentalArchitecture } from '../components/ProductNarrativeArchitecture'

<LessonChapter
  id="geo-verification"
  className="core-use-case-screen geo-verification-screen"
  eyebrow="03 / GEO 效果付费"
  title="GEO 按品牌被 AI 引用的提升效果付费"
  accent="用同一组问题复测，达到约定增量才结算"
  body="签约时确定测试问题、当前引用率和观察周期；由独立机构复测，品牌引用率提升达标后付款。"
  visual={<GeoVerificationArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="fog-inference"
  className="fog-screen"
  eyebrow="03 / 本地隐私计算"
  title="雾计算把 AI 运算放在数据所在地"
  accent="敏感数据留在工厂、医院或机构内部"
  body="a3s-box 在现场创建隔离运行环境，限制网络、文件和工具权限。外部只收到结果和可核验回执，看不到原始数据。"
  figureLabel="本地数据 → a3s-box 隔离执行 → 签名回执 → ACVM"
  visual={<FogInferenceArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="ans"
  className="ans-screen"
  eyebrow="03 / ANS 服务目录"
  title="ANS 帮助调用方找到可信的智能体服务"
  accent="像服务目录一样，先查身份和能力，再下单"
  body="每个智能体都发布带签名的服务卡，写明提供者、能力、接口、价格和有效期。调用方核验后，再通过 ACVM 创建任务。"
  figureLabel="签名服务卡 → 已核验端点 → ACVM 订单"
  visual={<AgentResolutionArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="agent-rental"
  className="agent-rental-screen"
  eyebrow="03 / 智能体租赁"
  title="把自己的智能体出租，按完成效果收费"
  accent="模型和私有数据不交付，只提供服务结果"
  body="所有者设定服务范围、验收标准和价格，租用方提交任务。结果通过验收后，ACVM 自动付款，并按约定给多方分账。"
  visual={<AgentRentalArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="simulation"
  className="core-use-case-screen simulation-screen"
  eyebrow="03 / 社会模拟服务"
  title="多家机构可以共同完成可信的社会模拟"
  accent="各自保留原始数据，只共享统计结果"
  body="参与方提前确定模型、样本范围和随机规则，在本地运行同一实验。ACVM 核对执行记录和汇总结果后，再完成结算。"
  visual={<SocialSimulationSimpleArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="poi-consensus"
  className="poi-consensus-screen"
  eyebrow="04 / PoI 共识算法"
  title="ACVM 把有效贡献换成公平的记账机会"
  accent="不同任务先统一评分，再限制单个参与方的影响"
  body="只有已经验收的 PoI 才能计分。不同任务先分类换算并设上限，再随机选出候选节点，由多方共同确认结果。"
  visual={<PoiConsensusArchitecture />}
>
</LessonChapter>

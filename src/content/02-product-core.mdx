import { GeoVerificationArchitecture, SocialSimulationSimpleArchitecture } from '../components/CoreUseCaseAnimations'
import { AgentResolutionArchitecture } from '../components/AgentResolutionArchitecture'
import { FogInferenceArchitecture } from '../components/FogInferenceArchitecture'
import { LessonChapter } from '../components/LessonChapter'
import { AgentRentalArchitecture } from '../components/ProductNarrativeArchitecture'

<LessonChapter
  id="geo-verification"
  className="core-use-case-screen geo-verification-screen"
  eyebrow="04 / GEO 效果付费"
  title="GEO 按 AI 引用提升效果付费"
  accent="链上智能体独立复测后结算"
  body="只按确认的引用增量付费。"
  detail="签约时把测试问题、当前引用率和观察周期写入订单。链上 ACVM 验证智能体调用独立观察节点取得查询回执，再按固定公式计算增量；通过后自动付款。"
  visual={<GeoVerificationArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="ans"
  className="ans-screen"
  eyebrow="04 / ANS 智能体网络"
  title="ANS 让用户找到可调用的智能体"
  accent="身份、能力、镜像和履历统一可查"
  body="解析签名服务卡，再创建 ACVM 调用任务。"
  detail="企业把 a3s-code 构建的智能体和 a3s-use 能力清单写入签名服务卡，ANS 公开其 DID、imageRoot、capabilityRoot、价格和 TEE 要求。用户解析并验签后创建 ACVM 任务；历史 PoI 继续用于能力评分、协作组队和奖励更新。"
  figureLabel="唯一身份 → 服务发布 → 用户发现 → 履历评分 → 协作与奖励"
  visual={<AgentResolutionArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="fog-inference"
  className="fog-screen"
  eyebrow="04 / 雾计算网络"
  title="雾计算按任务选择节点或专家组"
  accent="单节点隐私执行，MoE 专家并行"
  body="ANS 找到服务后，雾网络再匹配执行节点。"
  detail="用户通过 ANS 找到智能体服务后，ACVM 把任务交给雾计算网络。普通任务按 TEE、GPU、位置、负载和信誉选择单节点；MoE 任务选择同地域低时延专家组，路由器只调用 Top-k 专家。a3s-box 在各节点建立 TEE 边界，加密镜像、专家分片和激活值均受保护。"
  figureLabel="ANS 调用 → 节点或专家组筛选 → 远程证明 → TEE 隐私推理"
  visual={<FogInferenceArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="agent-rental"
  className="agent-rental-screen"
  eyebrow="04 / 智能体即服务"
  title="一笔智能体服务从发布走到分账"
  accent="ANS、雾计算和 ACVM 串成完整服务闭环"
  body="企业发布，用户调用，结果通过后自动分账。"
  detail="企业用 a3s-code 构建智能体，用 a3s-use 声明能力，再把加密镜像和服务卡发布到 ANS。用户创建任务后，雾网络选择节点；a3s-box 管理隔离执行，a3s-power 在 TEE 内完成隐私推理，链上验证通过后由 ACVM 分账。"
  visual={<AgentRentalArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="simulation"
  className="core-use-case-screen simulation-screen"
  eyebrow="04 / 社会模拟服务"
  title="多家机构共同完成可信社会模拟"
  accent="数据留本地，统计结果可验收"
  body="各机构按同一规则运行并安全汇总。"
  detail="参与方提前确定模型、样本范围和随机规则，在各自本地运行同一实验。只共享群体统计和置信区间，ACVM 核对执行记录与汇总过程后再完成结算。"
  visual={<SocialSimulationSimpleArchitecture />}
>
</LessonChapter>

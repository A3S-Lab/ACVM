import { AcvmExecutionBoundaryArchitecture } from '../components/AcvmExecutionBoundary'
import { LessonChapter } from '../components/LessonChapter'
import { UsefulWorkOpportunityArchitecture } from '../components/ProductMechanismArchitecture'

<LessonChapter
  id="useful-work"
  className="useful-work-screen"
  eyebrow="02 · WHY / CONSENSUS WORK"
  title="共识一定要烧哈希吗？"
  accent="不用，稀缺工作也可以服务客户"
  body="ACVM 用已验收的模型推理替代哈希工作来源。PoI 只增加提议机会；随机抽签和多数终局仍保留。"
  figureLabel="FIRST PRINCIPLE / SECURITY FUNCTION ≠ HASH FUNCTION"
  visual={<UsefulWorkOpportunityArchitecture />}
>
</LessonChapter>

<LessonChapter
  id="execution-boundary"
  className="acvm-execution-boundary-screen"
  eyebrow="02 · WHY / APPLICATION VM"
  title="为什么模型不能在链上重跑？"
  accent="每个节点验回执，不重复跑模型"
  body="模型依赖 GPU、私有数据和外部工具，结果也可能不完全一致。ACVM 让 Worker 执行一次，链上只验证证据和状态变化。"
  figureLabel="FIRST PRINCIPLE / REPLAY COST → RECEIPT VERIFICATION"
  visual={<AcvmExecutionBoundaryArchitecture />}
>
</LessonChapter>
